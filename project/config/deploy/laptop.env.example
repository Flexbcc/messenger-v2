# Copy once to laptop.env and fill in (never commit laptop.env).
GITEA_PASSWORD=your-flex-password
MAIN_HOST=root@194.67.92.147
WORKER_HOST=root@161.104.18.45
MAIN_IP=194.67.92.147
WORKER_IP=161.104.18.45
LAPTOP_SSH_KEY=$HOME/.ssh/messenger_ops

# Local control panel only (127.0.0.1:9300). Generate: ./scripts/setup-operator-secret.sh
OPERATOR_SECRET=

# One-time client join invites. Generate: ./scripts/setup-gateway-invite-secret.sh
# Must match GATEWAY_INVITE_SECRET in server .env (gateway-node).
GATEWAY_INVITE_SECRET=
CLUSTER_ID=default
