# Автогенерация: scripts/generate-operator-cert.sh
# Не редактируйте вручную — правьте operators.tsv и перезапустите скрипт.
#
# Отзыв доступа: удалите строку оператора из operators.tsv,
# выполните любой выпуск заново (или regenerate-allowlist), затем nginx -s reload.

map $ssl_client_fingerprint $operator_allowed {
    default 0;
    9a5f84a8885566315ef6e03a0572cc3c87ec949a 1;  # MacBook-Pro-3 (до Jul 28 08:27:55 2027 GMT)
}
