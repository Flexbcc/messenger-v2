# Открытые вопросы (обсудить позже)

Решённое перенесено в [`SETTINGS.md`](SETTINGS.md).

## Функциональность
- [ ] Чанкинг больших файлов (размер чанка, сборка, докачка).
- [ ] Оффлайн ПК: очередь/ретраи на ноде; деградация до media/S3 fallback.
- [ ] Резервирование: зеркалить блобы на второй ПК / в S3 (hybrid).
- [ ] Несколько дисков/корней (сейчас MVP — один `allowed_root`).

## Связность
- [ ] mDNS-обнаружение в LAN: формат сервиса, безопасность.
- [ ] Выбор relay и failover (см. simulation relay-failover).
- [ ] Постоянный канал vs коннект по требованию (энергия/трафик).

## Безопасность
- [ ] Точный механизм шифрования meta.db (SQLCipher vs прикладное).
- [ ] PIN/биометрия на подтверждение пары и на доступ к app.
- [ ] Формат кода/QR сопряжения, TTL, одноразовость.
- [ ] Revoke пары: подтверждение удаления папки пользователя.

## Политики
- [ ] TTL по created_at или last_access (или обоим); дефолтные сроки.
- [ ] Расписание GC-прохода; лимиты нагрузки при чистке.

## UX
- [ ] Онбординг: лимиты, автозапуск при старте ОС (tray).
- [ ] Экран статуса: детализация трафика/аудита.

## Интеграция
- [ ] Точная сигнатура backend `personal_pc` в media-node (base.py).

## Решено (см. SETTINGS.md)
- [x] Место хранения — выбор папки при онбординге, миграция при смене.
- [x] Файловая структура — users/<uuid>/blobs/<aa>/<bb>/<hash>.
- [x] At-rest — блобы как есть, meta.db шифруется.
- [x] БД — SQLite (WAL), метаданные + отдача стримом по хэшу.
- [x] Разграничение — папка на пользователя, per-user квоты.
- [x] Индикация/логи — tray + экран статуса + append-only audit_log.
- [x] Переполнение — reject (quota_exceeded), нода решает fallback.
